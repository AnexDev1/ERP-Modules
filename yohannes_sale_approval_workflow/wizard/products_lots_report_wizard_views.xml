<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_products_lots_report_wizard_form" model="ir.ui.view">
        <field name="name">products.lots.report.wizard.form</field>
        <field name="model">products.lots.report.wizard</field>
        <field name="arch" type="xml">
            <form string="Print Products Report">
                <group>
                    <field name="price_type" widget="radio"/>
                </group>
                <footer>
                    <button name="action_print_report" string="Print" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_products_lots_report_wizard" model="ir.actions.act_window">
        <field name="name">print product with pricelist</field>
        <field name="res_model">products.lots.report.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="stock.model_stock_quant"/>
        <field name="binding_view_types">list</field>
        <field name="binding_type">report</field>
    </record>

</odoo>
