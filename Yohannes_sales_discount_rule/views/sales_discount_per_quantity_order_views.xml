<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_discount_per_quantity_order_form" model="ir.ui.view">
        <field name="name">discount.per.quantity.order.form</field>
        <field name="model">discount.per.quantity.order</field>
        <field name="arch" type="xml">
            <form string="Discount Per Quantity Order">
                <header>
                    <button name="action_submit" string="Submit"
                            type="object" invisible="state != 'draft'"
                            group="yohannes_sale_approval_workflow.group_sales_discount_user"/>
                    <button name="action_approved" string="Approve"
                            type="object" invisible="state != 'submitted'"
                            group="yohannes_sale_approval_workflow.group_sales_discount_manager"/>
                    <button name="action_reject" type="object" string="Reject"
                            class="btn-secondary" invisible="state !='approved'"
                            groups="yohannes_sale_approval_workflow.group_sales_discount_manager"/>
                    <field name="state" widget="statusbar"/>

                </header>
                <sheet>
                    <group>


                        <group>
                            <field name="DPQ_reference"/>
                            <field name="payment_term_id"/>
                            <field name="from_quantity"/>
                            <field name="to_quantity"/>
                        </group>
                        <group>
                            <field name="product_id"/>
                            <field name="percentage"/>
                            <field name="company_id"/>
                        </group>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_discount_per_quantity_order_list" model="ir.ui.view">
        <field name="name">discount.per.quantity.order.list</field>
        <field name="model">discount.per.quantity.order</field>
        <field name="arch" type="xml">
            <list string="Discount Per Quantity Order">
                <field name="DPQ_reference"/>
                <field name="payment_term_id"/>
                <field name="product_id"/>
                <field name="from_quantity"/>
                <field name="to_quantity"/>
                <field name="company_id"/>
                <field name="percentage"/>
            </list>
        </field>
    </record>
</odoo>