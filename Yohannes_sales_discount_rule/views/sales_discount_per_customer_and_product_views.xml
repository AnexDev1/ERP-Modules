<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_discount_per_customer_and_product_form" model="ir.ui.view">
        <field name="name">discount.per.customer.and.product.form</field>
        <field name="model">discount.per.customer.and.product</field>
        <field name="arch" type="xml">
            <form string="Discount Per Customer And Product">
                <header>
                    <button name="action_submit" string="Submit"
                            type="object" invisible="state != 'draft'"
                            group="yohannes_sale_approval_workflow.group_sales_discount_user"/>
                    <button name="action_approved" string="Approve"
                            type="object" invisible="state != 'submitted'"
                            group="yohannes_sale_approval_workflow.group_sales_discount_manager"/>
                    <button name="action_reject" type="object" string="Reject"
                            class="btn-secondary" invisible="state !='approved'"
                            groups="yohannes_sale_approval_workflow.group_sales_discount_manager"/>
                    <field name="state" widget="statusbar"/>

                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="DPCP_reference"/>
                            <field name="payment_term_id"/>
                            <field name="product_id"/>
                        </group>
                        <group>
                            <field name="partner_id"/>
                            <field name="company_id"/>
                            <field name="percentage"/>
                        </group>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_discount_per_customer_and_product_list" model="ir.ui.view">
        <field name="name">discount.per.customer.and.product.list</field>
        <field name="model">discount.per.customer.and.product</field>
        <field name="arch" type="xml">
            <list string="Discount Per Customer and Product">
                <field name="DPCP_reference"/>
                <field name="payment_term_id"/>
                <field name="product_id"/>
                <field name="partner_id"/>
                <field name="company_id"/>
                <field name="percentage"/>
                <field name="state"/>
            </list>
        </field>
    </record>
</odoo>