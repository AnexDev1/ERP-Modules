<odoo>
    <record id="view_account_fiscal_period_tree" model="ir.ui.view">
        <field name="name">account.fiscal.period.tree</field>
        <field name="model">account.fiscal.period</field>
        <field name="arch" type="xml">
            <list decoration-muted="state=='closed'">
                <field name="fiscal_year_id"/>
                <field name="name"/>
                <field name="code"/>
                <field name="description"/>
                <field name="date_from"/>
                <field name="date_to"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'open'"
                       decoration-danger="state == 'closed'"
                       decoration-info="state == 'draft'"/>
            </list>
        </field>
    </record>

    <record id="view_account_fiscal_period_form" model="ir.ui.view">
        <field name="name">account.fiscal.period.form</field>
        <field name="model">account.fiscal.period</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_open" type="object" string="Set to Open"
                            invisible="state != 'draft'" class="btn-primary"/>
                    <button name="action_close" type="object" string="Close Period"
                            invisible="state == 'closed'" class="btn-primary"/>
                    <button name="action_reopen" type="object" string="Reopen"
                            invisible="state != 'closed'"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,open,closed"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="fiscal_year_id"/>
                            <field name="name"/>
                            <field name="code"/>
                            <field name="description"/>
                        </group>
                        <group>
                            <field name="date_from"/>
                            <field name="date_to"/>
                            <field name="company_id" groups="base.group_multi_company"/>
                        </group>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="action_account_fiscal_period" model="ir.actions.act_window">
        <field name="name">Fiscal Periods</field>
        <field name="res_model">account.fiscal.period</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_account_fiscal_period"
              name="Fiscal Periods"
              parent="account.menu_finance_configuration"
              action="action_account_fiscal_period"
              sequence="20"/>
</odoo>
