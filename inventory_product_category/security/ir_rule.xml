<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Rule to hide categories not in master from regular users -->
        <record id="rule_product_category_available_master" model="ir.rule">
            <field name="name">Droga: Categories Available in Master Only</field>
            <field name="model_id" ref="product.model_product_category"/>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="domain_force">['|', ('available_in_master', '=', True), ('id', '!=', False)]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>
    </data>
</odoo>
