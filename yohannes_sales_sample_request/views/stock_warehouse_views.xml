<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_warehouse_form_inherit_sample_consignment" model="ir.ui.view">
        <field name="name">stock.warehouse.form.inherit.sample.consignment</field>
        <field name="model">stock.warehouse</field>
        <field name="inherit_id" ref="stock.view_warehouse"/>
        <field name="arch" type="xml">
            <!-- Removed as per user request to use dynamic lookups instead of warehouse fields -->
        </field>
    </record>
</odoo>
